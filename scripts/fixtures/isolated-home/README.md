# Isolated regression fixtures

Synthetic sibling-tool layouts used by `scripts/lib/isolated-env.sh`.
Runtime seeding recreates these under a temp `HOME` — **never** the operator's
real home. Keep contents free of real usernames, tokens, or hostnames.
